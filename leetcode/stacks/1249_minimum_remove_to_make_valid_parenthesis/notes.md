# Reflection

1) I should have known that the string concatenation is `O(N^2)` time 

2) The only improvement is that the first loop is redundant, we would already handle the early closing brackets with the `if not stack` conditional
